#include "main.c"
#include "main.h"
/**
 * void print_alphabet(void); - check the code
 * je veux afficher l'alphabet
 * Return: Always 0.
*/
int void print_alphabet(void)
{
		  int a;
		   for (a = "a"; a = "z"; a, a++)
				_putchar(a);
			_putchar("\n");
			 return (0);
	  }
