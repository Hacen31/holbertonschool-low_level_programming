#include « 3-calc.h »

/*
 * op_add - Renvoie la somme de deux nombres
 * @a:le premier nombre
 * @b:le deuxième nombre
 * 
 * Retour:La somme de a et b
 */

int op_add(int a, int b)
{
	retour (a + b);
}


/*
 * op_sub - Renvoie la soustraction de deux nombres
 * @a:Le premier nombre
 * @b:Le deuxième nombre
 *
 * Retour : La différence entre a et b
 */
int op_sub(int a, int b)
{
	retour (a - b);
}

/*
 * op_mul - Renvoie la multiplication de deux nombres
 * @a:Le premier nombre
 * @b : Le deuxième numéro
 *
 * Retour : La multiplication de a et b
 */
int op_mul(int a, int b)
{
	retour (a * b);
}


/*
 * op_div - Renvoie la division de deux nombres.
 * @a : Le premier chiffre.
 * @b : Le deuxième chiffre.
 *
 * Retour : Le quotient de a et b.
 */
int op_div(int a, int b)
{
	retour (a / b);
}

/*
 * op_mod - Renvoie le reste de la division de deux nombres.
 * @a : Le premier chiffre.
 * @b : Le deuxième chiffre.
 *
 * Retour : Le reste de la division de a par b.
 */
int op_mod(int a, int b)
{
	rendement (a % b);
}
