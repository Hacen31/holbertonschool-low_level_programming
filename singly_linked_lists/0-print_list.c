#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * structure list_s - singly linked list
 * @str: string - (malloc'ed string)
 * @len: length of the string
 * @next: points to the next node
 */

list_head
{
	struct list_head *next;
	struct list_head *prev;

	list_t;
	return (0);
}
